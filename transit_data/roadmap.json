{
  "note": "Progress against the Phase 0 audit. Hand-maintained: mark items done only when verified, never when merely attempted.",
  "phases": [
    {
      "phase": "1 — Make it safe to change",
      "items": [
        {
          "id": "tests-static",
          "title": "Static test suite over the built artifact",
          "priority": "P0",
          "state": "done",
          "note": "18 checks, zero dependencies, mutation-verified 7/7"
        },
        {
          "id": "tests-browser",
          "title": "Browser smoke layer",
          "priority": "P0",
          "state": "done",
          "note": "10 checks in a real browser; proven to catch the never-laid-out map regression"
        },
        {
          "id": "ci-gate",
          "title": "Gate every publishing path on the suite",
          "priority": "P0",
          "state": "done",
          "note": "pages.yml plus all three data bots"
        },
        {
          "id": "telemetry",
          "title": "Error telemetry and window.onerror",
          "priority": "P0",
          "state": "done",
          "note": "Local fault capture + one-tap report; nothing is sent automatically, and reports carry no location, saved places or key"
        },
        {
          "id": "licence",
          "title": "LICENSE and third-party data terms",
          "priority": "P0",
          "state": "done",
          "note": "MIT for the code, DATA-SOURCES.md for the third-party terms; two ODbL/scraping questions flagged for a lawyer"
        },
        {
          "id": "csp",
          "title": "Content Security Policy",
          "priority": "P0",
          "state": "done",
          "note": "Hash-pinned inline script, no unsafe-inline or unsafe-eval in script-src, per-directive host coverage enforced by the suite"
        }
      ]
    },
    {
      "phase": "2 — Make it safe to grow",
      "items": [
        {
          "id": "modules",
          "title": "Split the monolith into source files",
          "priority": "P1",
          "state": "done",
          "note": "5,747 lines became 20 files in transit_data/src, concatenated into a byte-identical page."
        },
        {
          "id": "module-isolation",
          "title": "Full ES-module isolation",
          "priority": "P1",
          "state": "declined",
          "note": "Measured, then declined on the evidence. Zero collisions exist today, so the risk is unrealised, and a guard now fails the build if one is ever introduced — the same protection at a fraction of the cost. Conversion is genuinely feasible (the evaluation-time graph is acyclic; the only cycles run through function bodies, which ES modules resolve at call time), but it means exports and imports across 509 top-level names plus a bundler this machine cannot reliably install. Revisit if collisions start appearing, if more than one person is editing, or if code-splitting becomes necessary."
        },
        {
          "id": "payload",
          "title": "Shrink the 6.1 MB bus payload",
          "priority": "P1",
          "state": "done",
          "note": "Road geometry split into its own lazily-fetched file: the first bus interaction is 0.48 MB over the wire instead of 1.16 MB, and geometry loads only when a route is actually drawn"
        },
        {
          "id": "confidence",
          "title": "Per-record data confidence model",
          "priority": "P1",
          "state": "done",
          "note": "provenance.json declares source, licence, coverage and a confidence kind for all 29 shipped datasets; build.cjs stamps each file’s real modification time so freshness is measured, not claimed. Settings shows the list oldest-first, and the suite fails if a dataset the build reads is undeclared."
        },
        {
          "id": "bus-orphans",
          "title": "Close the 40 orphaned Istanbul bus refs",
          "priority": "P1",
          "state": "done",
          "note": "All 40 recovered: build.cjs reconstructs the missing directory rows from the graph, so nothing routable is unlistable. Ratchet retired for a clean assertion"
        }
      ]
    },
    {
      "phase": "3 — Depth over breadth",
      "items": [
        {
          "id": "city-depth",
          "title": "Bring İzmir, Bursa, Antalya to Kocaeli’s level",
          "priority": "P2",
          "state": "done",
          "note": "İzmir done at operator depth: 441 ESHOT lines, 882 directions, 62,269 stops and road geometry, from the city’s own portal; stop ORDER derived by projecting each line’s stops onto its own route shape. Bursa and Antalya publish no reachable open bus feed — probed with İzmir as a control, evidence in DATA-SOURCES.md — so they stay rail-only by evidence rather than by omission."
        },
        {
          "id": "search-rank",
          "title": "Search ranking: match quality, kind, interchange, distance",
          "priority": "P2",
          "state": "done",
          "note": "Replaced “prefix then substring, each alphabetically” with a score. Adds Turkish names for all 35 English-only landmarks (“Ayasofya” found bus stops before), category words, prefix coverage scaling, interchange degree and distance as a tie-break. “havalimanı” now returns the three airports instead of a taxi rank; “sahil” moves rail from rank 15 to 1. Enter-to-resolve shares the ranking, so the list and the field agree. ~2 ms/query over 6,463 places. Open-now deliberately not added: no opening-hours data is shipped, so it could only be invented."
        },
        {
          "id": "assistant-intents",
          "title": "Widen local assistant intent coverage",
          "priority": "P2",
          "state": "done",
          "note": "Offline parser now answers the questions its own LLM tools already could: service alerts (“is M2 running” used to return a route summary, which reads as a yes), step-free/lift data per station, which rail lines serve a stop, and “how long from A to B”. All from data already shipped, EN and TR. Fixing the alert path also exposed a translator bug that rendered “istasyonunda” as “stationnda”; the missing Turkish case endings are now handled and every rule is \b-anchored, so an unlisted inflection stays Turkish instead of becoming a non-word. Covered by transit_data/test/translator.test.cjs and 2 mutations."
        }
      ]
    }
  ]
}