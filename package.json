{
  "name": "raynet-transit-map",
  "private": true,
  "description": "Build pipeline and test suite for the RAY-NET transit map. The app itself ships as a single static index.html with no runtime dependencies.",
  "scripts": {
    "build": "node transit_data/build.cjs",
    "test": "node transit_data/testkit/run.cjs",
    "test:mutation": "node transit_data/testkit/verify-suite.cjs",
    "verify": "npm run build && npm test && npm run test:mutation && npm run status",
    "status": "node transit_data/gen-status.cjs",
    "scope": "node transit_data/testkit/analyse-scope.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
